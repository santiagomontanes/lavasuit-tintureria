# URL del backend LavaSuit (sin /api ni barra final).
# - En desarrollo (mismo PC): http://localhost:3000
# - En producción (PC con backend en la red local): http://IP_DEL_SERVIDOR:3000
# - Si el backend está en internet: https://api.tu-dominio.com
#
# Para que un cambio aquí surta efecto hay que volver a buildear (npm run build).

VITE_API_URL=http://localhost:3000
